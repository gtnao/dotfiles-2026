{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; if echo \"$f\" | grep -q '\\.lua$'; then ~/.local/share/nvim/mason/bin/stylua \"$f\"; fi; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
